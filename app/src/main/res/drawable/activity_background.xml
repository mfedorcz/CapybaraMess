<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary"/>
        </shape>
    </item>z

    <!-- Watermark Image at the bottom-right corner -->
    <item android:drawable="@drawable/watermark" android:gravity="bottom|left">
        <bitmap android:src="@drawable/watermark"
            android:gravity="bottom|left"
            android:width="100dp"
            android:height="100dp"/>
    </item>
</layer-list>
